version: '3.7'

services:
  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - /home/student24/fullchain.pem:/etc/ssl/certs/fullchain.pem:ro
      - /home/student24/privkey.pem:/etc/ssl/private/privkey.pem:ro
      - /home/student24/nginx/vhost.d:/etc/nginx/vhost.d
      - /home/student24/nginx/html:/usr/share/nginx/html
    environment:
      - DEFAULT_HOST=forteam22ict.xyz

  react-flask-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: react-flask-app
    environment:
      - VIRTUAL_HOST=forteam22ict.xyz
      - VIRTUAL_PORT=3000
    volumes:
      - .:/usr/src/app
      - $WORKSPACE/react-flask-app:/usr/src/app/react-flask-app
    expose:
      - "3000"
      - "5000"

volumes:
  jenkins_home:
